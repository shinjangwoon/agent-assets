# 에이전트 명세 틀

```markdown
# <에이전트 이름>

## 등록 설정
| 항목 | 값 |
|---|---|
| 플랫폼 | Multica / … |
| 런타임 · 모델 | |
| 호출 권한 | private / workspace |
| 배정 스킬 | |
| 배정 MCP | |

## 역할
한 문단. 무엇을 하고 무엇을 하지 않는지.

## 호출 경로
어떻게 깨어나는지 (채팅 / 코멘트 멘션 / 배정 / 웹훅). 경로마다 동작이 다르면 나눠 쓴다.

## 동작
단계별. 외부 호출이 있으면 URL 은 `<webhook-url>` 플레이스홀더.

## 규칙
반드시 / 절대. 추측 금지 항목.

## 허용 · 금지
"외부 API 금지" 같은 포괄 금지를 쓰면 필요한 웹훅까지 막힌다. 허용 대상을 먼저 명시하고 나머지를 금지한다.

## 응답 형식
```
