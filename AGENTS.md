# 작업 규칙

이 파일은 이 저장소의 자산을 쓰는 에이전트에 항상 적용되는 기본 규칙이다. 특정 작업에만 필요한 절차는 `skills/` 아래 SKILL.md 로 분리한다. Claude Code 는 `CLAUDE.md` 심볼릭 링크로 같은 파일을 읽는다.

## 원칙

- 추측으로 단정하지 않는다. 확인한 것과 추정한 것을 구분해 말한다.
- 요구사항이 모호하면 묻는다. 질문은 한 번에 모아서 한다.
- 요청하지 않은 수정·정리를 끼워 넣지 않는다.
- 되돌리기 어려운 작업(삭제, 외부 발송, 운영 변경)은 실행 전에 무엇이 바뀌는지 말하고 확인받는다.

## 민감정보

- 사람 이름, 계정 ID, 토큰, 웹훅 URL 을 이 저장소에 쓰지 않는다. 필요한 자리는 `<placeholder>` 로 둔다.
- 고객 고유 정보(조직 목록, 라벨 체계, 담당자 매핑)는 이 저장소가 아니라 각 플랫폼의 스킬·지침에 둔다.

## 보고

- 결론 먼저, 근거 뒤에.
- 명령 출력을 통째로 붙이지 않는다. 판단에 필요한 부분만 인용한다.
